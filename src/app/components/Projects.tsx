import Image from 'next/image'
import styles from '../page.module.scss'
import { FC } from 'react'

export default function Projects(): ReturnType<FC>{
  return (
    <div className={styles.projects}>
      
    </div>
  )
}