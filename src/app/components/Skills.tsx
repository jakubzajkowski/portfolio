import Image from 'next/image'
import styles from '../page.module.scss'
import { FC } from 'react'

export default function Skills(): ReturnType<FC> {
  return (
    <div className={styles.skills}>
      
    </div>
  )
}